local Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield'))()
    Rayfield:LoadConfiguration()

local Window = Rayfield:CreateWindow({
   Name = "World Zero Launcher",
   Icon = 0, -- Icon in Topbar. Can use Lucide Icons (string) or Roblox Image (number). 0 to use no icon (default).
   LoadingTitle = "World Zero Launcher Interface",
   LoadingSubtitle = "By Oreo",
   ShowText = "", -- for mobile users to unhide Rayfield, change if you'd like
        Theme = {
            -- TEXT
            TextColor = Color3.fromRGB(245, 225, 190),
            PlaceholderColor = Color3.fromRGB(140, 120, 95),

            -- BACKGROUNDS
            Background = Color3.fromRGB(18, 10, 5),
            Topbar = Color3.fromRGB(30, 15, 8),
            Shadow = Color3.fromRGB(12, 6, 3),

            NotificationBackground = Color3.fromRGB(22, 12, 6),
            NotificationActionsBackground = Color3.fromRGB(180, 140, 80),

            -- TABS
            TabBackground = Color3.fromRGB(40, 22, 12),
            TabStroke = Color3.fromRGB(120, 85, 45), -- glow stroke
            TabBackgroundSelected = Color3.fromRGB(160, 110, 55),
            TabTextColor = Color3.fromRGB(245, 225, 190),
            SelectedTabTextColor = Color3.fromRGB(30, 15, 8),

            -- ELEMENTS
            ElementBackground = Color3.fromRGB(32, 18, 10),
            ElementBackgroundHover = Color3.fromRGB(40, 22, 12),
            SecondaryElementBackground = Color3.fromRGB(22, 12, 6),

            -- Glow Strokes (inner + outer)
            ElementStroke = Color3.fromRGB(255, 200, 120),      -- bright gold glow
            SecondaryElementStroke = Color3.fromRGB(95, 65, 35), -- inner bronze

            -- SLIDERS
            SliderBackground = Color3.fromRGB(80, 55, 30),
            SliderProgress = Color3.fromRGB(220, 170, 90),
            SliderStroke = Color3.fromRGB(255, 210, 130),

            -- TOGGLES (Glow Enabled)
            ToggleBackground = Color3.fromRGB(28, 16, 8),

            ToggleEnabled = Color3.fromRGB(240, 190, 110),        -- glowing gold
            ToggleDisabled = Color3.fromRGB(90, 70, 55),

            ToggleEnabledStroke = Color3.fromRGB(255, 220, 150),  -- bright outer glow
            ToggleDisabledStroke = Color3.fromRGB(110, 85, 65),

            ToggleEnabledOuterStroke = Color3.fromRGB(140, 100, 60), -- bronze halo
            ToggleDisabledOuterStroke = Color3.fromRGB(70, 50, 35),

            -- DROPDOWNS
            DropdownSelected = Color3.fromRGB(45, 25, 12),
            DropdownUnselected = Color3.fromRGB(30, 18, 10),

            -- INPUTS
            InputBackground = Color3.fromRGB(28, 16, 8),
            InputStroke = Color3.fromRGB(255, 200, 120) -- glowing gold border
        },

   ToggleUIKeybind = "K", -- The keybind to toggle the UI visibility (string like "K" or Enum.KeyCode)

   DisableRayfieldPrompts = false,
   DisableBuildWarnings = false, -- Prevents Rayfield from emitting warnings when the script has a version mismatch with the interface.

   -- ScriptID = "sid_xxxxxxxxxxxx", -- Your Script ID from developer.sirius.menu — enables analytics, managed keys, and script hosting

   ConfigurationSaving = {
      Enabled = true,
      FolderName = nil, -- Create a custom folder for your hub/game
      FileName = "WorldZeroHub"
   },

   Discord = {
      Enabled = true, -- Prompt the user to join your Discord server if their executor supports it
      Invite = "https://discord.gg/vXgbVmajMZ", -- The Discord invite code, do not include Discord.gg/. E.g. Discord.gg/ABCD would be ABCD
      RememberJoins = false -- Set this to false to make them join the Discord every time they load it up
   },

   KeySystem = false, -- Set this to true to use our key system
   KeySettings = {
      Title = "World Zero Hub",
      Subtitle = "World Zero Launcher Key System Interface",
      Note = "Join The Discord to get Access to our 'Daily Changing Keys'", -- Use this to tell the user how to get a key
      FileName = "Key", -- It is recommended to use something unique, as other scripts using Rayfield may overwrite your key file
      SaveKey = true, -- The user's key will be saved, but if you change the key, they will be unable to use your script
      GrabKeyFromSite = false, -- If this is true, set Key below to the RAW site you would like Rayfield to get the key from
      Key = {"https://raw.githubusercontent.com/Cap4Teen4/WZ-Hub/main/key.txt"} -- List of keys that the system will accept, can be RAW file links (pastebin, github, etc.) or simple strings ("hello", "key22")
   }
})

local Tele = Window:CreateTab("Teleport Tab", "radio-tower")
local Tps = Tele:CreateSection("Tele Tab")
Tps:Set("World Teleports")


-- World 1
local W1 = Tele:CreateButton({
   Name = "World 1",
   Callback = function()
             Rayfield:Notify({
            Title = "Teleport to World 1",
            Content = "Successful Teleport",
            Duration = 8.5,
            Image = "radio-tower",
        })
        wait(3)
      game.ReplicatedStorage.Shared.Teleport.TeleportToHub:FireServer(13)
   end,
})
W1:Set("World 1")

-- World 2
local W2 = Tele:CreateButton({
   Name = "World 2",
   Callback = function()
             Rayfield:Notify({
            Title = "Teleport to World 2",
            Content = "Successful Teleport",
            Duration = 8.5,
            Image = "radio-tower",
        })
        wait(3)
      game.ReplicatedStorage.Shared.Teleport.TeleportToHub:FireServer(19)
   end,
})
W2:Set("World 2")

-- World 3
local W3 = Tele:CreateButton({
   Name = "World 3",
   Callback = function()
            Rayfield:Notify({
            Title = "Teleport to World 3",
            Content = "Successful Teleport",
            Duration = 8.5,
            Image = "radio-tower",
        })
        wait(3)
      game.ReplicatedStorage.Shared.Teleport.TeleportToHub:FireServer(20)
   end,
})
W3:Set("World 3")

-- World 4
local W4 = Tele:CreateButton({
   Name = "World 4",
   Callback = function()
            Rayfield:Notify({
            Title = "Teleport to World 4",
            Content = "Successful Teleport",
            Duration = 8.5,
            Image = "radio-tower",
        })
        wait(3)
      game.ReplicatedStorage.Shared.Teleport.TeleportToHub:FireServer(29)
   end,
})
W4:Set("World 4")

-- World 5
local W5 = Tele:CreateButton({
   Name = "World 5",
   Callback = function()
            Rayfield:Notify({
            Title = "Teleport to World 5",
            Content = "Successful Teleport",
            Duration = 8.5,
            Image = "radio-tower",
        })
        wait(3)
      game.ReplicatedStorage.Shared.Teleport.TeleportToHub:FireServer(31)
   end,
})
W5:Set("World 5")

-- World 6
local W6 = Tele:CreateButton({
   Name = "World 6",
   Callback = function()
            Rayfield:Notify({
            Title = "Teleport to World 6",
            Content = "Successful Teleport",
            Duration = 8.5,
            Image = "radio-tower",
        })
        wait(3)
      game.ReplicatedStorage.Shared.Teleport.TeleportToHub:FireServer(36)
   end,
})
W6:Set("World 6")

-- World 7
local W7 = Tele:CreateButton({
   Name = "World 7",
   Callback = function()
            Rayfield:Notify({
            Title = "Teleport to World 7",
            Content = "Successful Teleport",
            Duration = 8.5,
            Image = "radio-tower",
        })
        wait(3)
      game.ReplicatedStorage.Shared.Teleport.TeleportToHub:FireServer(40)
   end,
})
W7:Set("World 7")
-- World 8
local W8 = Tele:CreateButton({
   Name = "World 8",
   Callback = function()
            Rayfield:Notify({
            Title = "Teleport to World 8",
            Content = "Successful Teleport",
            Duration = 8.5,
            Image = "radio-tower",
        })
        wait(3)
      
   end,
})
W8:Set("World 8")


local Tab = Window:CreateTab("Main Tab", "crown")

-- SECTION 1: Quick Hubs
local QuickHubs = Tab:CreateSection("Quick Hubs")

local Button = Tab:CreateButton({
   Name = "Dex",
   Callback = function()
           Rayfield:Notify({
            Title = "Launching Dex",
            Content = "Successful launched Dex",
            Duration = 8.5,
            Image = "badge-check",
        })
        wait(2)
   loadstring(game:HttpGet("https://raw.githubusercontent.com/peyton2465/Dex/master/out.lua"))()
   end,
})
Button:Set("Dex")
local QuickHubs = Tab:CreateDivider()

local Button2 = Tab:CreateButton({
   Name = "Zero Hub",
   Callback = function()
              Rayfield:Notify({
            Title = "Launching Zero Hub",
            Content = "Successful launched Zero Hub",
            Duration = 8.5,
            Image = "badge-check",
        })
        wait(2)
   loadstring(game:HttpGet("https://raw.githubusercontent.com/justdoingrandomstuff/World-Zero/main/autofarm"))()
   end,
})
Button2:Set("Zero Hub")
local QuickHubs = Tab:CreateDivider()

local Button3 = Tab:CreateButton({
   Name = "Remote Spy v1",
   Callback = function()
           Rayfield:Notify({
            Title = "Launching Remote Spy v1",
            Content = "Successful launched Remote Spy v1",
            Duration = 8.5,
            Image = "badge-check",
        })
        wait(2)
   loadstring(game:HttpGet("https://raw.githubusercontent.com/Cap4Teen4/RemoteSpy/refs/heads/main/RemoteSpy"))()
   end,
})
Button3:Set("Remote Spy v1")
local QuickHubs = Tab:CreateDivider()

-- SECTION 2: Special GUIs
local SpecialGUIs = Tab:CreateSection("Special GUIs")
-- Open Sell
local Button3 = Tab:CreateButton({
   Name = "Rejoin Server",
    Callback = function()
            Rayfield:Notify({
            Title = "Tester",
            Content = "Successful Test",
            Duration = 8.5,
            Image = "badge-check",
            })
            while wait(2) do
            for _,v in pairs(game:GetService("Workspace").ScavengerHunts.EggHunt._ScavengerHuntContainer:GetDescendants()) do
                if v:IsA("TouchTransmitter") then
                firetouchinterest(game.Players.LocalPlayer.Character.HumanoidRootPart, v.Parent, 0)
                wait()
             firetouchinterest(game.Players.LocalPlayer.Character.HumanoidRootPart, v.Parent, 1)
           end
        end
    end 
    end,
})
Button3:Set("Rejoin Server")







local HubSetting = Window:CreateTab("Settings Tab", "cog")
local HubSet = HubSetting:CreateSection("Setting Tab")
HubSet:Set("World Zero Hub Settings")
local CloseButton = HubSetting:CreateDivider()

local CloseButton = HubSetting:CreateButton({
    Name = "Close Hub",
    Callback = function()
        Rayfield:Notify({
            Title = "Closed Hub",
            Content = "Successful Closed Hub",
            Duration = 8.5,
            Image = "circle-x",
        })
        wait(5)
        Rayfield:Destroy()
    end,
})
local CloseButton = HubSetting:CreateDivider()

local ReloadButton = HubSetting:CreateButton({
    Name = "Reload Hub",
    Callback = function()
        Rayfield:Notify({
            Title = "Reloaded Hub",
            Content = "Successful Reloading Hub",
            Duration = 5.5,
            Image = "circle-x",
        })
        wait(1)
        loadstring(game:HttpGet("https://raw.githubusercontent.com/Cap4Teen4/WZ-Hub/refs/heads/Hub/WZLauncher.lua"))()
    end,
})
local CloseButton = HubSetting:CreateDivider()
local CloseButton = HubSetting:CreateLabel("Made by 42Oreobun", 4483362458, Color3.fromRGB(120, 85, 45), false) -- Title, Icon, Color, IgnoreTheme
